<!-- stem -->
Always work through the stem skill: size the work, keep
docs/planning/ current, verify before done.
Now: no active initiative. Career timeline rebuilt across all seven roles (Feb 2020 — Present, 6.5y, continuous full-time chain) and the favicon ships as icon.svg + favicon.ico + apple-icon.png; both verified, awaiting commit. Awaiting Oleksii's yes on two judgement calls in the career data: the ADPRODIGIES pair was swapped so the promotion reads forward, and locations follow one Ukraine→Poland move. Note: `origin` uses a read-only key — push via the `github-deploy-website` alias.
<!-- /stem -->
