<!-- stem -->
Always work through the stem skill: size the work, keep
docs/planning/ current, verify before done.
Now: no active initiative. Portfolio labels, menu sheet, Velar wordmark and the header CTA shipped in 2e87687 and pushed. Note: `origin` uses a read-only key — push via the `github-deploy-website` alias.
<!-- /stem -->
