<!-- stem -->
Always work through the stem skill: size the work, keep
docs/planning/ current, verify before done.
Now: no active initiative. Mobile burger menu built and verified (Nav.tsx, globals.css, SmoothScroll exports getLenis) — uncommitted, awaiting Oleksii's yes, on top of the equally uncommitted portfolio-grid rebuild and Hero/Velar redesign. Note: `origin` uses a read-only key — push via the `github-deploy-website` alias.
<!-- /stem -->
